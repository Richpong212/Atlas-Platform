apiVersion: v1
kind: Service
metadata:
  name: atlas-api
  labels:
    app.kubernetes.io/name: atlas-api
    app.kubernetes.io/part-of: atlas-platform
spec:
  selector:
    app.kubernetes.io/name: atlas-api
  ports:
    - name: http
      port: 80
      targetPort: 3303
  type: ClusterIP
